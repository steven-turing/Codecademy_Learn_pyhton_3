# We can check the type of a Python variable using the type() function.A variable’s type determines what you can do
# with it and how you can use it.
print(type(5))

my_dict = {}
print(type(my_dict))

my_list = []
print(type(my_list))
